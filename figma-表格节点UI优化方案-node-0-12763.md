# 京东金融 B 端表格 UI 优化方案（node `0:12763`）

来源：[Figma - 京东金融B端组件库（表格）](https://www.figma.com/design/8fQIquokFWJQblXRBgc0yN/%E4%BA%AC%E4%B8%9C%E9%87%91%E8%9E%8DB%E7%AB%AF%E7%BB%84%E4%BB%B6%E5%BA%93?node-id=0-12763&t=q0YWQnNNHF4GfxQq-4)

## 1. 本次已落地优化（代码已改）

目标文件：`client/src/components/ui/table.tsx`

- 表格容器升级为白底卡片容器，增加统一边框：`border-[#ECEFF3]`、`rounded-md`。
- 统一表格基础文字：`14px` / `22px`，贴近设计稿主文案密度。
- 表头背景调整为浅灰：`#F7F8FA`，并统一分割线颜色：`#ECEFF3`。
- 行高统一到 `48px`（`h-12`）以提升数据可读性和对齐一致性。
- 行 hover / 选中态改为浅蓝反馈：`#F5F8FF` / `#EEF2FE`。
- 单元格水平内边距统一：`16px`（`px-4`）。
- Caption 次要文字色调整为 `#8C8C8C`。

## 2. 对齐到设计的关键样式基线

- 主文字色：`#191919`（对应 `rgb(25, 25, 25)`）
- 品牌主色：`#2C68FF`（对应 `rgb(44, 104, 255)`）
- 常用边框：`#ECEFF3`、`#DCDCDC`
- 表头/背景层次：`#F7F8FA`、`#FAFAFA`
- 圆角主值：`4px`（当前表格容器保留圆角，兼容已有组件体系）

## 3. 下一步可继续优化（可选）

- 增加“可调整列宽”交互（匹配设计稿“可调整列宽表格”）。
- 增加“显示隐藏字段”列配置弹层（匹配“设置显示字段”）。
- 增加空态、加载态、错误态统一插槽（匹配“空状态/数据状态”）。
- 增加操作列 `更多` 的下拉菜单规范样式。
- 为表格封装 `density`（`compact/default/comfortable`）以适配不同页面密度。
